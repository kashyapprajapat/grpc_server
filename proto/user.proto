syntax = "proto3";

service Userservice{
    rpc addUser(User) returns (User) {} 
    rpc getUsers(Empty) returns (Users){}
}


message Empty {}

message User{
    string name = 1;
    string email = 2;
    int64 age = 3; 
}

message Users{
    repeated User users = 1;
}